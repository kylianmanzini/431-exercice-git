# 04 — Comparaisons et booléens

x = 10
y = 5

# TODO: compléter
egal = (x==y)
different = (x!=y)
plus_grand = (x > y)
logique = (x != y) or (x < y > 0)  # exemple: (x > y) and (y > 0)

if __name__ == "__main__":
    print(egal, different, plus_grand, logique)
