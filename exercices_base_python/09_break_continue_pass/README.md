# 09 — break, continue, pass

Objectifs:
- Comprendre et utiliser `break`, `continue`, `pass` dans des boucles et fonctions.

Consignes:
- Écrire des boucles qui utilisent `break` et `continue`. Placer `pass` dans une fonction vide.
