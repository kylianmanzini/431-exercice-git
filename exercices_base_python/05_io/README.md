# 05 — Entrées / sorties

Objectifs:
- Lire avec `input()` et afficher avec `print()`.

Théorie:
- `input()` renvoie une chaîne; convertir si nécessaire.

Consignes:
- Demander un nom et un âge, puis afficher un message formaté.
